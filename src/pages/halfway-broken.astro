---
import SiteHeader from "../components/SiteHeader.astro";
import SiteFooter from "../components/SiteFooter.astro";
const title = "Halfway Broken — Ken Pham";
---
<!doctype html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width,initial-scale=1" />
  <meta name="theme-color" content="#000000" />
    <title>{title}</title>
    <meta name="description" content="Halfway Broken - A film by Ken Pham." />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <link rel="canonical" href="https://www.kenpham.ca/halfway-broken" />
  <!-- Plausible Analytics -->
  <script defer data-domain="www.kenpham.ca" src="https://plausible.io/js/script.js"></script>
    
  <!-- Open Graph / Twitter -->
  <meta property="og:site_name" content="Ken Pham" />
  <meta property="og:type" content="website" />
  <meta property="og:title" content="Halfway Broken — Ken Pham" />
  <meta property="og:description" content="Halfway Broken - A film by Ken Pham." />
  <meta property="og:url" content="https://www.kenpham.ca/halfway-broken" />
  <meta name="twitter:card" content="summary_large_image" />
  <meta name="twitter:title" content="Halfway Broken — Ken Pham" />
  <meta name="twitter:description" content="Halfway Broken - A film by Ken Pham." />
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link rel="preload" as="style" href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&display=swap">
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&display=swap" rel="stylesheet">

    <style>
      :root { 
        --font-primary: 'Inter', -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Arial, sans-serif;
      }
      
      html, body { 
        margin: 0; 
        background: #000000;
        color: #f2f2f2;
        font-family: var(--font-primary);
        -webkit-font-smoothing: antialiased;
      }
      
      /* Custom scrollbar */
      ::-webkit-scrollbar {
        width: 12px;
      }
      ::-webkit-scrollbar-track {
        background: #000000;
      }
      ::-webkit-scrollbar-thumb {
        background: #111111;
        border-radius: 6px;
        border: 2px solid #000000;
      }
      ::-webkit-scrollbar-thumb:hover {
        background: #222222;
      }
      * {
        scrollbar-width: thin;
        scrollbar-color: #111111 #000000;
      }      
      .wrap { max-width:1200px; margin:0 auto; padding:16px; }
      
      @media (max-width: 768px) {
        .wrap { padding: 24px; }
      }
      .site-header { 
        text-align: left;
        margin-bottom: 48px;
        padding: 32px 0;
        position: relative;
      }
      
      .menu-toggle {
        display: none;
        position: absolute;
        top: 32px;
        right: 0;
        background: none;
        border: none;
        cursor: pointer;
        padding: 8px;
        z-index: 100;
      }
      
      .menu-toggle span {
        display: block;
        width: 24px;
        height: 2px;
        background: #f2f2f2;
        margin: 5px 0;
        transition: 0.3s;
      }
      
      .main-nav {
        position: absolute;
        top: 42px;
        right: 0;
        display: flex;
        gap: 32px;
        align-items: center;
      }
      .main-nav a {
        color: rgba(242, 242, 242, 0.6);
        text-decoration: none;
        font-size: 14px;
        font-weight: 400;
        letter-spacing: 0.3px;
        transition: color 0.2s ease;
        text-transform: lowercase;
      }
      .main-nav a:hover {
        color: #fff;
        text-decoration: underline;
        text-underline-offset: 6px;
        outline: none;
      }
      .main-nav a:focus {
        color: #fff;
        text-decoration: underline;
        text-underline-offset: 6px;
        outline: none;
      }
      .main-nav a.active {
        color: #f2f2f2;
        text-decoration: underline;
        text-underline-offset: 4px;
      }
      .site-header h1 {
        margin: 0;
        font-size: 32px;
        font-weight: 700;
        letter-spacing: 0.5px;
        color: #f2f2f2;
      }
      .site-header h1 a {
        color: #f2f2f2;
        text-decoration: none;
        transition: opacity 0.2s ease;
      }
      .site-header h1 a:hover {
        opacity: 0.7;
      }
      .header-title {
        display: flex;
        align-items: center;
        gap: 16px;
      }
      .social-icons {
        display: flex;
        gap: 12px;
        align-items: center;
      }
      .social-icons a {
        color: rgba(242, 242, 242, 0.7);
        transition: color 0.2s ease;
        display: flex;
        align-items: center;
        text-decoration: none;
      }
      .social-icons a.imdb-link {
        font-size: 11px;
        font-weight: 700;
        letter-spacing: 0.2px;
        border: 1px solid rgba(242, 242, 242, 0.7);
        border-radius: 3px;
        padding: 1px 5px;
        line-height: 1.2;
        transition: all 0.2s ease;
      }
      .social-icons a.imdb-link:hover {
        border-color: #f2f2f2;
      }
      .social-icons a:hover {
        color: #fff;
        outline: none;
        filter: drop-shadow(0 0 2px #fff2);
      }
      .social-icons a:focus {
        color: #fff;
        outline: none;
        filter: drop-shadow(0 0 2px #fff2);
      }
      .social-icons svg {
        width: 20px;
        height: 20px;
      }
      .site-header h2 {
        margin: 8px 0 0 0;
        font-size: 14px;
        font-weight: 400;
        letter-spacing: 0.2px;
        color: #f2f2f2;
      }
      
      @media (max-width: 768px) {
        .site-header {
          text-align: center;
          padding: 24px 0;
          margin-bottom: 32px;
        }
        
        .site-header h1 {
          font-size: 24px;
        }
        
        .site-header h2 {
          font-size: 13px;
        }
        
        .header-title {
          justify-content: center;
          flex-wrap: wrap;
          gap: 12px;
        }
        
        .social-icons {
          gap: 16px;
        }
        
        .menu-toggle {
          display: block;
        }
        
        .main-nav {
          position: fixed;
          top: 0;
          right: -100%;
          width: 70%;
          height: 100vh;
          background: #000000;
          flex-direction: column;
          justify-content: center;
          align-items: center;
          gap: 32px;
          transition: right 0.3s ease;
          z-index: 99;
        }
        
        .main-nav.active {
          right: 0;
        }
        
        .main-nav a {
          font-size: 18px;
        }
        
        .project-hero {
          flex-direction: column;
          gap: 32px;
          margin-bottom: 48px;
        }
        
        .project-hero-content {
          order: 1;
        }
        
        .project-hero-poster {
          order: 2;
          width: 100%;
        }

        .project-hero h2 {
          font-size: 36px;
          margin-bottom: 12px;
        }
        
        .project-meta {
          font-size: 14px;
          margin-bottom: 24px;
        }

        .project-synopsis {
          font-size: 15px;
          line-height: 1.7;
          margin-bottom: 32px;
        }
        
        .project-synopsis p {
          margin-bottom: 16px;
        }
        
        .laurel {
          margin-bottom: 32px;
        }
        
        .laurel img {
          max-width: 100%;
        }
        
        .project-credits {
          margin-bottom: 48px;
        }
        
        .project-credits h3 {
          font-size: 28px;
          margin-bottom: 32px;
        }
        
        .credits-list {
          grid-template-columns: 1fr;
          gap: 16px;
        }

        .gallery-grid {
          grid-template-columns: 1fr;
          gap: 16px;
        }
        
        .site-footer {
          margin-top: 64px;
          padding: 32px 0;
        }
      }

      .project-hero {
        margin-bottom: 64px;
        display: flex;
        gap: 48px;
        align-items: start;
      }

      .project-hero-content {
        flex: 1;
      }

      .project-hero-poster {
        flex-shrink: 0;
        width: 576px;
      }

      .project-hero-poster img {
        width: 100%;
        height: auto;
      }

      .project-hero h2 {
        font-size: 48px;
        font-weight: 700;
        margin: 0 0 16px 0;
        letter-spacing: -1px;
      }

      .project-meta {
        font-size: 16px;
        color: rgba(242, 242, 242, 0.7);
        margin-bottom: 32px;
      }

      .project-synopsis {
        font-size: 16px;
        line-height: 1.8;
        margin-bottom: 6px;
        max-width: 800px;
      }

      .project-synopsis p {
        margin-bottom: 20px;
        color: rgba(242, 242, 242, 0.9);
      }

      .project-cast {
        margin-top: 6px;
        margin-bottom: 36px;
        display: flex;
        flex-direction: column;
        gap: 2px;
      }

      .cast-label {
        color: rgba(242, 242, 242, 0.6);
        font-size: 12px;
        text-transform: uppercase;
        letter-spacing: 0.5px;
      }

      .cast-names {
        color: #f2f2f2;
        font-weight: 600;
        font-size: 14px;
      }

      .laurel {
        margin-bottom: 48px;
      }

      .laurel img {
        max-width: 200px;
        height: auto;
      }

      .project-credits {
        margin-bottom: 64px;
      }

      .project-credits h3 {
        font-size: 32px;
        font-weight: 700;
        margin: 0 0 48px 0;
        letter-spacing: 0.5px;
        text-align: left;
      }

      .credits-list {
        display: grid;
        grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
        gap: 12px;
        font-size: 14px;
        line-height: 1.6;
      }

      .credit-item {
        display: flex;
        flex-direction: column;
        gap: 2px;
      }

      .credit-role {
        color: rgba(242, 242, 242, 0.6);
        font-size: 12px;
        text-transform: uppercase;
        letter-spacing: 0.5px;
      }

      .credit-name {
        color: #f2f2f2;
        font-weight: 600;
        font-size: 14px;
      }

      .press-link {
        color: rgba(242, 242, 242, 0.6);
        text-decoration: none;
        font-size: 14px;
        font-weight: 400;
        letter-spacing: 0.3px;
        transition: color 0.2s ease;
        text-transform: none;
        position: relative;
        display: inline-block;
        padding: 0;
        margin: 0;
        border: none;
        background: none;
        max-width: 100%;
      }

      .press-link .underline {
        position: absolute;
        bottom: -2px;
        left: 0;
        right: 0;
        height: 1px;
        background: #fff;
        transform: scaleX(0);
        transform-origin: left;
        transition: transform 0.6s cubic-bezier(.4,0,.2,1);
        width: 100%;
        pointer-events: none;
      }
      .press-link:hover .underline,
      .press-link:focus-visible .underline {
        transform: scaleX(1);
      }

      .project-gallery {
        margin-bottom: 64px;
      }

      .project-gallery h3 {
        font-size: 32px;
        font-weight: 700;
        margin: 0 0 48px 0;
        letter-spacing: 0.5px;
        color: #f2f2f2;
      }

      .gallery-grid {
        display: grid;
        grid-template-columns: repeat(3, 1fr);
        gap: 24px;
      }

      @media (max-width: 1100px) {
        .gallery-grid {
          grid-template-columns: repeat(2, 1fr);
        }
      }

      @media (max-width: 700px) {
        .gallery-grid {
          grid-template-columns: 1fr;
        }
      }

      .gallery-grid img {
        width: 100%;
        height: auto;
        display: block;
        transition: opacity 0.3s;
      }

      .gallery-grid img:hover {
        opacity: 0.9;
      }

      .back-link {
        display: inline-block;
        position: relative;
        background: rgba(38, 38, 38, 0.95);
        color: #f2f2f2;
        padding: 10px 18px;
        border-radius: 6px;
        text-decoration: none;
        font-size: 10px;
        font-weight: 700;
        letter-spacing: 0.8px;
        text-transform: uppercase;
        text-align: center;
        margin-bottom: 32px;
        transition: transform 0.8s ease;
        box-shadow: 0 2px 8px rgba(0, 0, 0, 0.15);
        transform: translateY(0);
      }

      .back-link:hover {
        transform: translateY(-2px);
      }

      .back-link:focus-visible {
        outline: 2px solid #fff;
        outline-offset: 4px;
        border-radius: 6px;
        transform: translateY(-2px);
      }
      
      /* Lightbox styles */
      .lightbox {
        display: none;
        position: fixed;
        inset: 0;
        background: rgba(0, 0, 0, 0.95);
        z-index: 9999;
        align-items: center;
        justify-content: center;
        padding: 20px;
      }
      .lightbox.active {
        display: flex;
      }
      .lightbox img {
        max-width: 90vw;
        max-height: 90vh;
        object-fit: contain;
        border-radius: 4px;
      }
      .lightbox-close,
      .lightbox-prev,
      .lightbox-next {
        position: fixed;
        background: rgba(255, 255, 255, 0.1);
        border: none;
        color: #fff;
        font-size: 24px;
        width: 48px;
        height: 48px;
        border-radius: 50%;
        cursor: pointer;
        transition: background 0.2s ease;
        display: flex;
        align-items: center;
        justify-content: center;
        z-index: 10000;
      }
      .lightbox-close:hover,
      .lightbox-prev:hover,
      .lightbox-next:hover {
        background: rgba(255, 255, 255, 0.2);
      }
      .lightbox-close {
        top: 20px;
        right: 20px;
      }
      .lightbox-prev {
        left: 20px;
        top: 50%;
        transform: translateY(-50%);
      }
      .lightbox-next {
        right: 20px;
        top: 50%;
        transform: translateY(-50%);
      }
      .gallery-grid img {
        cursor: pointer;
      }

    .skip-link {
      position: absolute;
      left: -999px;
      top: auto;
      width: 1px;
      height: 1px;
      overflow: hidden;
    }
    .skip-link:focus {
      position: fixed;
      left: 16px;
      top: 16px;
      width: auto;
      height: auto;
      padding: 10px 14px;
      background: #fff;
      color: #000;
      border-radius: 4px;
      z-index: 1000;
      text-decoration: none;
      border: 1px solid #0002;
      box-shadow: 0 2px 8px #0006;
    }
    
    /* View Transitions API */
    @view-transition {
      navigation: auto;
    }
    
    ::view-transition-old(root) {
      animation: fadeOut 0.3s ease;
    }
    
    ::view-transition-new(root) {
      animation: fadeIn 0.3s ease;
      background: #000000;
    }
    
    @keyframes fadeOut {
      to { opacity: 0; }
    }
    
    @keyframes fadeIn {
      from { opacity: 0; }
      to { opacity: 1; }
    }
    </style>

    <script type="application/ld+json">
    {
      "@context": "https://schema.org",
      "@type": "Movie",
      "name": "Halfway Broken",
      "genre": "Fiction",
      "director": {
        "@type": "Person",
        "name": "Ken Pham",
        "url": "https://www.kenpham.ca"
      }
    }
    </script>
  </head>
  <body>
    <a href="#main-content" class="skip-link">Skip to content</a>
    <div class="wrap">
      <SiteHeader />

  <main id="main-content">
        <a href="/" class="back-link">
          Back to Portfolio
        </a>
        
        <section class="project-hero">
          <div class="project-hero-content">
            <h2>HALFWAY BROKEN<br>BY LUNA ELLE</h2>
            <div class="project-meta">Music Video | 2 Minutes | Colour</div>
            <div class="project-synopsis">
            </div>
            <div class="laurel">
              <img src="/portfolio/halfwaybroken/cropped-1.-TIMVF-logo-PNG.png" alt="Festival Laurel" loading="lazy" />
            </div>
          </div>
          <div class="project-hero-poster">
            <picture>
              <source srcset="/portfolio/halfwaybroken/HalfwayBroken_Timeline_FINAL-COLOUR2_01_02_46_21.webp" type="image/webp" />
              <img src="/portfolio/halfwaybroken/HalfwayBroken_Timeline_FINAL-COLOUR2_01_02_46_21.jpg" alt="Halfway Broken Hero" loading="lazy" />
            </picture>
          </div>
        </section>

        <section class="project-credits">
          <h3>CREDITS</h3>
          <div class="credits-list">
            <div class="credit-item">
              <span class="credit-role">Director</span>
              <span class="credit-name">Ken Pham</span>
            </div>
            <div class="credit-item">
              <span class="credit-role">Producer</span>
              <span class="credit-name">Ken Asafo, Hotfreestyle</span>
            </div>
            <div class="credit-item">
              <span class="credit-role">Director of Photography</span>
              <span class="credit-name">Owain Parri</span>
            </div>
            <div class="credit-item">
              <span class="credit-role">Production Design</span>
              <span class="credit-name">Michaela Kurimsky</span>
            </div>
            <div class="credit-item">
              <span class="credit-role">Hair and Makeup</span>
              <span class="credit-name">Gabrielle Jovellanos</span>
            </div>
            <div class="credit-item">
              <span class="credit-role">Special Thanks</span>
              <span class="credit-name">Rebecca Kwan, Ian Simon and Broadview Flower Market</span>
            </div>
          </div>
        </section>

        <section class="project-gallery">
          <h3>STILLS</h3>
          <div class="gallery-grid">
            <picture>
              <source srcset="/portfolio/halfwaybroken/HalfwayBroken_Timeline_FINAL-COLOUR2_01_00_06_02.webp" type="image/webp" />
              <img src="/portfolio/halfwaybroken/HalfwayBroken_Timeline_FINAL-COLOUR2_01_00_06_02.jpg" alt="Halfway Broken - Still 1" loading="lazy" />
            </picture>
            <picture>
              <source srcset="/portfolio/halfwaybroken/HalfwayBroken_Timeline_FINAL-COLOUR2_01_00_24_17.webp" type="image/webp" />
              <img src="/portfolio/halfwaybroken/HalfwayBroken_Timeline_FINAL-COLOUR2_01_00_24_17.jpg" alt="Halfway Broken - Still 2" loading="lazy" />
            </picture>
            <picture>
              <source srcset="/portfolio/halfwaybroken/HalfwayBroken_Timeline_FINAL-COLOUR2_01_01_01_20.webp" type="image/webp" />
              <img src="/portfolio/halfwaybroken/HalfwayBroken_Timeline_FINAL-COLOUR2_01_01_01_20.jpg" alt="Halfway Broken - Still 3" loading="lazy" />
            </picture>
            <picture>
              <source srcset="/portfolio/halfwaybroken/HalfwayBroken_Timeline_FINAL-COLOUR2_01_01_27_13.webp" type="image/webp" />
              <img src="/portfolio/halfwaybroken/HalfwayBroken_Timeline_FINAL-COLOUR2_01_01_27_13.jpg" alt="Halfway Broken - Still 4" loading="lazy" />
            </picture>
            <picture>
              <source srcset="/portfolio/halfwaybroken/HalfwayBroken_Timeline_FINAL-COLOUR2_01_01_53_07.webp" type="image/webp" />
              <img src="/portfolio/halfwaybroken/HalfwayBroken_Timeline_FINAL-COLOUR2_01_01_53_07.jpg" alt="Halfway Broken - Still 5" loading="lazy" />
            </picture>
            <picture>
              <source srcset="/portfolio/halfwaybroken/HalfwayBroken_Timeline_FINAL-COLOUR2_01_01_59_05.webp" type="image/webp" />
              <img src="/portfolio/halfwaybroken/HalfwayBroken_Timeline_FINAL-COLOUR2_01_01_59_05.jpg" alt="Halfway Broken - Still 6" loading="lazy" />
            </picture>
            <picture>
              <source srcset="/portfolio/halfwaybroken/HalfwayBroken_Timeline_FINAL-COLOUR2_01_02_50_17.webp" type="image/webp" />
              <img src="/portfolio/halfwaybroken/HalfwayBroken_Timeline_FINAL-COLOUR2_01_02_50_17.jpg" alt="Halfway Broken - Still 7" loading="lazy" />
            </picture>
          </div>
        </section>
      </main>

      <SiteFooter />
    </div>

    <!-- Lightbox -->
    <div id="lightbox" class="lightbox">
      <button class="lightbox-close" aria-label="Close lightbox">✕</button>
      <button class="lightbox-prev" aria-label="Previous image">‹</button>
      <button class="lightbox-next" aria-label="Next image">›</button>
      <img id="lightbox-img" src="" alt="">
    </div>

    <!-- Menu toggle handled inside SiteHeader component -->
  </body>
  <button id="backToTop" aria-label="Back to top" tabindex="0" style="display:none;position:fixed;bottom:32px;right:32px;z-index:200;background:#000000cc;color:#fff;border:none;border-radius:50%;width:48px;height:48px;box-shadow:0 2px 12px #0007;cursor:pointer;transition:opacity 0.2s;outline:none;font-size:24px;align-items:center;justify-content:center;">
    ↑
  </button>
  <style>
    #backToTop:focus { outline: 2px solid #fff; }
    #backToTop[hidden] { display: none !important; }
  </style>
  <script>
    // Back to Top button logic
    (function(){
      const btn = document.getElementById('backToTop');
      function toggleBtn() {
        if(window.scrollY > 300) { btn.style.display = 'flex'; btn.style.opacity = '1'; } else { btn.style.opacity = '0'; setTimeout(()=>{btn.style.display='none';},200); }
      }
      window.addEventListener('scroll', toggleBtn);
      btn.addEventListener('click', ()=>{ window.scrollTo({top:0,behavior:'smooth'}); btn.blur(); });
      btn.addEventListener('keydown', e=>{ if(e.key==='Enter'||e.key===' '){ e.preventDefault(); window.scrollTo({top:0,behavior:'smooth'}); btn.blur(); }});
    })();
    
    // Reading progress bar
    (function(){
      const progressBar = document.getElementById('progress-bar');
      function updateProgress() {
        const scrollTop = window.scrollY;
        const docHeight = document.documentElement.scrollHeight - window.innerHeight;
        const progress = (scrollTop / docHeight) * 100;
        progressBar.style.width = progress + '%';
      }
      window.addEventListener('scroll', updateProgress);
      updateProgress();
    })();
    
    // Lightbox gallery
    (function(){
      const lightbox = document.getElementById('lightbox');
      const lightboxImg = document.getElementById('lightbox-img');
      const closeBtn = document.querySelector('.lightbox-close');
      const prevBtn = document.querySelector('.lightbox-prev');
      const nextBtn = document.querySelector('.lightbox-next');
      const galleryImages = document.querySelectorAll('.gallery-grid img');
      let currentIndex = 0;
      
      function openLightbox(index) {
        currentIndex = index;
        const img = galleryImages[index];
        lightboxImg.src = img.src;
        lightboxImg.alt = img.alt;
        lightbox.classList.add('active');
        document.body.style.overflow = 'hidden';
      }
      
      function closeLightbox() {
        lightbox.classList.remove('active');
        document.body.style.overflow = '';
      }
      
      function showPrev() {
        currentIndex = (currentIndex - 1 + galleryImages.length) % galleryImages.length;
        openLightbox(currentIndex);
      }
      
      function showNext() {
        currentIndex = (currentIndex + 1) % galleryImages.length;
        openLightbox(currentIndex);
      }
      
      galleryImages.forEach((img, index) => {
        img.addEventListener('click', () => openLightbox(index));
      });
      
      closeBtn.addEventListener('click', closeLightbox);
      prevBtn.addEventListener('click', showPrev);
      nextBtn.addEventListener('click', showNext);
      
      lightbox.addEventListener('click', (e) => {
        if (e.target === lightbox) closeLightbox();
      });
      
      document.addEventListener('keydown', (e) => {
        if (!lightbox.classList.contains('active')) return;
        if (e.key === 'Escape') closeLightbox();
        if (e.key === 'ArrowLeft') showPrev();
        if (e.key === 'ArrowRight') showNext();
      });
    })();
  </script>
</html>
